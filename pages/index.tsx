import { Calendar } from '~/components';

const Home = () => {
    return (
        <main className="w-full h-page grid place-items-center">
            <Calendar />
        </main>
    );
};

export default Home;
